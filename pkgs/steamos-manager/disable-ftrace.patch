diff --git a/src/daemon/root.rs b/src/daemon/root.rs
index 02d97ea..48b629f 100644
--- a/src/daemon/root.rs
+++ b/src/daemon/root.rs
@@ -120,8 +120,8 @@ impl DaemonContext for RootContext {
         self.state = state;

         let connection = daemon.get_connection();
-        let ftrace = Ftrace::init(connection).await?;
-        daemon.add_service(ftrace);
+        // let ftrace = Ftrace::init(connection).await?;
+        // daemon.add_service(ftrace);

         self.reload_ds_inhibit(daemon).await?;
