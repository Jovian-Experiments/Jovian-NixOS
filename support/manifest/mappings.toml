# This file contains mappings used by the `compare.py` script
# to match versions between upstream packages and ours.
#
# Each package can be marked as "ignore", "check" or "map".
# - "ignore"d packages will simply be excluded from all comparisons
# - "check"ed packages are checked against a fixed version string,
#     and generate an alert when the version is out of date.
# - "map"ped packages are checked against a version of a package
#     in the repository; the package name can be specified by
#     providing a string (or a list of strings) instead of "true"
#
# In addition, packages can set "pkgrel" to automatically add a pkgrel
# to the downstream version for the purposes of checking - this should
# be updated when our packaging is verified against an upstream update.
#
# "map"ped packages can also specify a "fixup" - a special hardcoded
# workaround that post-processes the version strings. See `compare.py`
# for the current list of fixups, and try not to add more to it.
#
# As a general guideline, we ignore things we don't expect to ever use,
# check things that may be relevant but aren't currently, or that aren't
# packaged, and map things that are packaged.
#
# This list is roughly sorted, but probably doesn't need to be maintained
# that way.

[packages]
# rebuild
7zip.ignore = true

# we use our own upstream version
# FIXME: maybe port Dualsense config from Valve
alsa-ucm-conf.check = "1.2.14-2.4"

# Valve A/B stuff
atomupd-daemon.ignore = true

# devkit specific patches
avahi.ignore = true

# backport from 6.5
bluedevil.check = "1:6.4.3-2"

# FIXME: has some not-yet-upstream patches, maybe we want them in nixpkgs?
bluez.check = "5.86-4.1"

# more A/B stuff
bmap-tools.ignore = true

breakpad = { map = true, pkgrel = 1, fixup = "v" }

# Installer???
calamares.ignore = true
calamares-settings-steamos-git.ignore = true

# more A/B stuff
casync.ignore = true

# FIXME: ship this once the pipewire side is ready
cec-audio-control.ignore = true

cecd.map = true

# why?
ckbcomp.ignore = true

# just disables tests
clang.check = "20.1.8-1.1"

# rebuilds with no changes
cups.ignore = true

# unused
debos.ignore = true

# A/B stuff
desync.ignore = true

# enables SteamOS A/B stuff
discover.check = "6.4.3-1.5"

# SteamOS disk encryption thing
dirlock.ignore = true

# we don't need this
dkms.ignore = true

# foreground booster
dmemcg-booster.map = true

# non-critical
# FIXME: package
ds-inhibit.ignore = true

# FIXME: dev tool, not packaged currently
drm_info.ignore = true
drm_info-git.ignore = true

# FIXME: GPU reset thingy, package?
drm_janitor.ignore = true

# does magic telemetry we don't want
earlyoom.ignore = true

# we use nixpkgs version
f3.ignore = true

# Arch stuff
filesystem.ignore = true

# not relevant to us
firewalld.ignore = true

# rebuild
flatpak.check = "1:1.16.6-1.1"

# FIXME: some internal Valve thing, investigate?
foxnet.ignore = true
foxnetstatsd.ignore = true

# we use nixpkgs version
fwupd-minimal.ignore = true

galileo-mura = { map = true, pkgrel = 3, fixup = "v" }

gamescope.map = ["gamescope", "gamescope-session"]

# FIXME: dev tool
gpu-trace.ignore = true

# we don't use it
grub.ignore = true

# we use nixos config
holo-dmi-rules.check = "1.1-1"

holo-earlyoom.check = "1.1-1"

# unused, check in case it ever becomes used again
holo-fstab-repair.check = "0.2-1"

# the split out locales
holo-glibc-locales.check = "2.41+r65+ge7c419a29575-1"

# we don't use those repos
holo-keyring.ignore = true

# immutable root stuff
holo-nfs-utils-tmpfiles.ignore = true

# lul
holo-nix-offload.ignore = true

# FIXME: needs matching plymouth
holo-plymouth-themes.check = "0.5-1"

# FIXME: investigate
holo-rust-packaging-tools.ignore = true

# applied as NixOS config
holo-zram-swap.check = "0.3-0"

# backport
hwdata.check = "0.397-1.1"

# FIXME: IME stuff
ibus.ignore = true
ibus-anthy.ignore = true
ibus-pinyin.ignore = true
ibus-table-cangjie-lite.ignore = true

inputattach-cec-units.map = true

inputplumber.map = true

# some non-critical patches
iwd.check = "3.9-1.1"

jupiter-dock-updater-bin = { map = true, pkgrel = 1 }

jupiter-fan-control = { map = true, pkgrel = 4 }

# we have our own, better one
jupiter-firewall.ignore = true

jupiter-hw-support = { map = ["jupiter-hw-support", "steamdeck-hw-theme", "steamdeck-firmware", "holo-polkit-helpers"], pkgrel = 1 }

jupiter-legacy-support.check = "1.157-1"

# Valve telemetry stuff
jupiter-steamos-log-submitter.ignore = true

# FIXME: what is this?
jupiter-validation-tools.ignore = true

# backports
kcoreaddons.check = "6.16.0-1.2"

# foreground booster
kcgroups.check = "0.0.dmemcg.experimental.3-1"

# security backport
kdeconnect.check = "25.04.3-1.1"

# debug stuff
kdump-steamos.ignore = true
kdumpst.ignore = true

# just packaging changes
kmod.check = "34.2-1.1"

# hack for hidpi on internal display
kwin.check = "6.4.3-1.9"
kwin-x11.check = "6.4.3-1.2"

# backports
kwindowsystem.check = "6.16.0-1.1"

libdisplay-info.check = "0.3.0-1"

libdrm.check = "2.4.129-1.1"

# custom cursed patches
libei.check = "1.4.1-3"

# DisplayLink stuff
libevdi.ignore = true

# random rebuild
libpng.ignore = true

# some internal tooling, not shipped
libversion.ignore = true

# security backport
libxml2.check = "2.14.5-1.1"

linux-firmware-neptune = { map = "linux-firmware-jupiter", pkgrel = 1 }

# some other mainline kernel build
linux-lts.ignore = true

# old kernel
linux-neptune.ignore = true

# old kernel
linux-neptune-61.ignore = true

# old kernel
linux-neptune-65.ignore = true

# debug kernels
linux-neptune-65-kasan.ignore = true
linux-neptune-rtw-debug.ignore = true

# old kernel
linux-neptune-68.ignore = true

# old kernel
linux-neptune-611.ignore = true

# old kernel
linux-neptune-615.ignore = true

# old kernel
linux-neptune-616.ignore = true

# new kernel
linux-neptune-618 = { map = "linux_jovian", pkgrel = 1 }

# we don't identify as SteamOS
lsb-release.ignore = true

# packaging changes, removes a heavy dependency
maliit-keyboard.check = "2.3.1-4.2"

mangohud.check = "0.8.3.rc1.r24.g33c2c7dd-1"

mesa = { map = "mesa-radeonsi-jupiter", pkgrel = "3", fixup = "mesa" }
mesa-radv = { map = "mesa-radv-jupiter", pkgrel = "1", fixup = "mesa" }

# we don't actually apply any of this, but it's worth checking
# FIXME?
networkmanager.check = "1.52.1-1.2"

# just the ladspa plugin
noisetorch.check = "0.12.2.r47.g6d895f3-2"

openssl.check = "3.5.6-1.1"

# backport, we don't have bandwidth to deal with a11y right now :(
orca.ignore = true

# Arch stuff
pacman-contrib.ignore = true
paru.ignore = true

pcre2.check = "10.47-1.1"

# patches for CEC volume control, not upstreamed yet, not worth it
pipewire.check = "1:1.6.2-1.1"

# builds from master branch for Qt6
# FIXME: do we want this?
plasma-remotecontrollers.check = "5.26.90.r258.g9ace9a9-2"

# packaging changes for SteamOS A/B
plasma-workspace.check = "6.4.3-1.3"

# FIXME: lots of patches, need to investigate
plymouth.check = "22.02.122-7.2"

# packaging changes only, will probably not be relevant ever
podman.ignore = true

# renamed to steamos-powerbuttond
powerbuttond.ignore = true

# steamos-specific hack
powerdevil.ignore = true

# some internal tooling, not shipped
python-libversion.ignore = true

# FIXME: more IME stuff
pyzy.ignore = true

# more A/B stuff
rauc.ignore = true

# debugging tools
renderdoc.ignore = true
renderdoc-minimal.ignore = true

rtkit.check = "0.14-1"

# unused
rtl88x2ce-dkms.ignore = true

scx-scheds.check = "1.1.0.linux.steamos-1"

# FIXME: package this
steam-im-modules.ignore = true

steam-jupiter-stable = { map = "steam-unwrapped", pkgrel = "4" }

# we don't use this
steam-jupiter-oobe.ignore = true

steam_notif_daemon = { map = true, pkgrel = 3, fixup = "v" }

steamdeck-dsp = { map = true, pkgrel = 1 }

# we don't ship those but they may be useful to reference
steamdeck-kde-presets.check = "3.8.4-1"

# very old, unused, but might be interesting? 
steamfs-git.ignore = true

# more A/B stuff
steamos-atomupd-client.ignore = true

# applied as NixOS modules instead
# check both to make sure we get the latest changes
steamos-customizations-git.check = "20260420.1-1"
steamos-customizations-jupiter.check = "20260420.1-1"

# devkit stuff
steamos-devkit-service.ignore = true

# A/B bootloader
steamos-efi.ignore = true

# debug stuff
steamos-kdumpst-layer.ignore = true
steamos-log-submitter.ignore = true

steamos-manager.map = true

# not relevant to us
steamos-media-creation-git.ignore = true

# Allows switching between iwd and wpa_supplicant wifi backends from Steam
# FIXME: doesn't seem to be wired up?
steamos-networking-tools.check = "1.2-1"

# some A/B thing
steamos-passwd.ignore = true

steamos-powerbuttond.map = "powerbuttond"

# more A/B stuff
steamos-repair-backend-git.ignore = true
steamos-repair-tool-git.ignore = true
steamos-reset.ignore = true

# more telemetry
steamos-systemreport.ignore = true

# applied as NixOS config
steamos-tweak-mtu-probing.check = "1-1"

# SteamOS A/B specific patch
systemd.check = "257.7-2.3"

# lul
ttf-twemoji-default.ignore = true

# security backport
udisks2.check = "2.10.1-6.1"

# debug tool
umr.ignore = true

# disables shutdown on battery critical, we probably don't want to do that
# without way more validation
upower.check = "1.90.10-1.1"

# non-critical but would be nice to have
# FIXME: package these
usbhid-gadget-passthru.ignore = true

# more precise battery monitoring
vpower.map = true

# no longer used
wakehook.ignore = true

wireplumber.map = "wireplumber-jupiter"

# flatpak backport dep
xdg-dbus-proxy.check = "0.1.7-1.1"

# non-critical fix backports
xdg-desktop-portal.check = "1.20.4-1.1"

xdg-desktop-portal-gamescope = { map = true, pkgrel = 1 }
xdg-desktop-portal-holo = { map = true, pkgrel = 1 }

# ours is newer
xdg-desktop-portal-gtk.ignore = true

# packaging changes + backports for small cosmetic issues
xorg-server.check = "21.1.22-1.1"
xorg-xwayland.check = "24.1.10-1.2"

# Arch packaging fix, we don't care
zenity-gtk3.check = "3.44.2-4"
